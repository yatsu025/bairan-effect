services:
  zopubairan:
    build: .
    ports:
      - "${PORT:-3001}:3001"
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "1"